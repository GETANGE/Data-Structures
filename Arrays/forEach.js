// keyword foreEach() which takes a callback function
// commonly used when you want to loop through all elements in a given array
// Does not return new elemets/ array.
const food = ['🍎' ,'🥕', '🍐', '🍆', '🍌', '🍓', '🥬', '🥑' ]

// SYNTAX: array.forEach()

food.forEach((element) =>{
    console.log(element)
})
